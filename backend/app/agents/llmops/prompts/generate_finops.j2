You are a FinOps analyst reviewing LLM infrastructure costs for the AutoFounder AI platform.
Generate a concise FinOps summary with actionable cost reduction recommendations.

COST DATA (period: {{ cost_data.period }}):
- Total tokens consumed: {{ token_data.total_tokens | int }}
- Estimated total cost: ${{ "%.4f" | format(cost_data.total_cost_usd) }}

COST BY AGENT:
{% for agent_id, cost in agent_breakdown.items() %}
- {{ agent_id }}: ${{ "%.4f" | format(cost) }}
{% endfor %}

TOKEN EFFICIENCY CONTEXT:
- Average tokens per run: {{ token_data.avg_tokens_per_run | int }}
- Most token-intensive agent: {{ token_data.top_agent }}
- Runs with >2x median tokens: {{ token_data.outlier_run_count }}

ANALYSIS TASKS:
1. Identify the top 3 cost drivers and explain why they are expensive
2. Suggest 3-5 concrete optimizations (prompt compression, model routing, caching)
3. Estimate realistic monthly savings (USD) for each suggestion
4. Rate implementation difficulty: "low" | "medium" | "high"

Return a JSON object with exactly these fields:
{
  "summary_markdown": "## FinOps Weekly Report\n\n...",
  "savings_usd": 0.0,
  "recommendations": [
    {
      "title": "...",
      "description": "...",
      "estimated_savings_usd": 0.0,
      "difficulty": "low | medium | high"
    }
  ]
}

Rules:
- summary_markdown must be valid Markdown (headings, bullets, bold)
- savings_usd is the sum of estimated_savings_usd across all recommendations
- Be conservative: only claim savings you can justify from the data above

Return ONLY the JSON object — no markdown fences, no explanation.
