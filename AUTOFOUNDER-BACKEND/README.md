# AutFounder-AI Enterprise — Backend

FastAPI + LangGraph API (planned). Phase 1 provides the Python package layout and **Ruff** lint baseline only.

See repository root `README.md` for local stack and quality commands.